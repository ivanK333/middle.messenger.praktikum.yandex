import { Input, Button, ButtonBack } from '../../components';

export type Props = {
  oldPassword: Input,
  newPassword: Input,
  reEnterNewPassword: Input,
  save: Button,
  buttonBack: ButtonBack,
};
