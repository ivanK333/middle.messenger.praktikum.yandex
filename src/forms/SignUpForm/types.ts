import { Input, Button, Link } from '../../components';

export type Props = {
  email: Input,
  login: Input,
  firstName: Input,
  secondName: Input,
  phone: Input,
  password: Input,
  repeatPassword: Input,
  signUp: Button,
  signIn: Link,
};
