import { Input } from '../Input';
import { ButtonSend } from '../ButtonSend';

export type Props = {
  className?: string,
  classNameImg?: string,
  input?: Input,
  button?: ButtonSend,
  img?: string,
};
