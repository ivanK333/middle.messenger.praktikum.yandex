import { InputChat } from '../InputChat';
import { ButtonSend } from '../ButtonSend';

export type Props = {
  className?: string,
  classNameImg?: string,
  input?: InputChat,
  button?: ButtonSend,
  img?: string,
};
