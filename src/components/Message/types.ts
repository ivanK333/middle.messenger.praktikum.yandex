export type Props = {
  className?: string,
  classNameWrap?: string,
  classNameMessage?: string,
  classNameDate?: string,
  classNameInfo?: string,
  isOutgoing?: boolean,
  message: string,
  check?: string,
  date: string,
};
