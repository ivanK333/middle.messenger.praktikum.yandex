import { Avatar } from '../Avatar';
import { HeaderCreateActiveSettingsButton } from '.';

export type Props = {
  className?: string,
  classNameName?: string,
  classNameNameAvatar?: string,
  name: string,
  avatar?: Avatar,
  setting?: HeaderCreateActiveSettingsButton,
};
