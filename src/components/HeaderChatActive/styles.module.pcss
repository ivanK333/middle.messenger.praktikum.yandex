@import "../../styles/_variables.pcss";
@import "../../styles/_mixins.pcss";

.header {
    @mixin flexbox center, flex-start;
    padding: 12px 16px;
    border-bottom: solid 1px $color-line;
}

.name {
    @mixin typography-text-xl;

    margin-left: 12px;
}